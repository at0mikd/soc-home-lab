# Wazuh manager overrides.
#
# Most settings live in the image defaults. This file is intentionally
# minimal; the goal is to have a hook for future tunables (alert levels,
# rule testing, log size) without bloating the image. Populate as the lab
# progresses.

# Example (commented) of where a tunable would go:
# logcollector.level=3
