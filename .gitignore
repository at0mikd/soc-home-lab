# --- Harness de Claude Code: herramienta LOCAL, no se publica ---
/CLAUDE.md
/.claude/
/plantillas/
/docs/guia-de-voz.md
/docs/etica.md

# --- Secretos: nunca al repo ---
.env
*.env
*.key
*.pem
secrets/
credentials*

# --- Certificados generados por el script oficial de Wazuh (ver setup/README.md).
# Se generan en local; no se versionan. Si los rotás, se regeneran.
setup/conf/manager_ssl_certs/

# --- Capturas o dumps con datos sensibles (revisar antes de quitar) ---
*-raw.png
dumps/

# --- Screenshots del lab: pendientes de revisar y limpiar antes de publicar ---
/screenshots/
